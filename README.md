# PraticaCoding

Este repositório é dedicado a projetos e exercícios práticos de programação.

## 🚀 Projeto: hello-rn

`hello-rn` é uma aplicação simples em React Native desenvolvida como um exercício prático.

### Objetivo

O objetivo deste projeto é adaptar o conteúdo e a estrutura de um portfólio web para uma versão mobile nativa, focando nos componentes básicos do React Native, como `<View>`, `<Text>`, `<ScrollView>` e `<Image>`.

### Tecnologias Utilizadas

* **React Native**
* **Expo**
* **TypeScript**

### Como Executar

Dentro da pasta `hello-rn`:

1.  **Instale as dependências:**
    ```bash
    npm install
    ```

2.  **Inicie o servidor de desenvolvimento:**
    ```bash
    npm start
    ```
    Ou use os comandos específicos:
    * `npm run android`
    * `npm run ios`
    * `npm run web`

## 📄 Licença

Este projeto está licenciado sob a Licença MIT.
